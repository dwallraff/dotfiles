IFS=$'\n' &&
for i in $(ls $HOME/code); do
    cd $HOME/code/$i;
    if [[ -d ".git" ]]; then
        printf "Updating $i\n";
        cd $HOME/code/$i; git pull;
        printf "\n";
    fi
done && printf "Updating brew\n" && brew update && brew upgrade